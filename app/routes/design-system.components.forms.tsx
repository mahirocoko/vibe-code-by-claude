import { Typography } from '@/components/ui/typography'

export default function FormsShowcase() {
  return (
    <div className="space-y-8">
      <div>
        <Typography variant="h1" className="text-3xl font-bold mb-2">Forms</Typography>
        <Typography className="text-muted-foreground">Form components and patterns (coming soon).</Typography>
      </div>

      <div className="rounded-lg border border-dashed p-8 text-center">
        <Typography className="text-muted-foreground">Form components will be added here</Typography>
      </div>
    </div>
  )
}
